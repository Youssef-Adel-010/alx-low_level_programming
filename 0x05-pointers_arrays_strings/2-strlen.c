#include "main.h"
/**
 * _strlen - function that returns the length of a string.
 *
 * @s: is a parameter.
 *
 * Return: int the length.
*/
int _strlen(char *s)
{
	int i, c = 0;

	for (i = *s; i != '\0'; i++)
	{
		c++;
	}
}
