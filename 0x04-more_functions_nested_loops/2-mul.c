#include "main.h"

/**
 * mul - function that multiplies two integers.
 *
 * @a: is the first parameter
 * @b: is the socend parameter
 *
 * Return: the result.
 *
*/

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
